import 'package:flutter/material.dart';

class CompletedProjectsScreenWidget extends StatelessWidget {
  const CompletedProjectsScreenWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Completed Projects Screen'),
      ),
    );
  }
}
